name: 逻辑表达训练推送

on:
  schedule:
    - cron: '30 8 * * *'
  workflow_dispatch:

jobs:
  push:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: 设置Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      
      - name: 安装依赖
        run: pip install requests
      
      - name: 推送逻辑表达训练
        env:
          SENDKEY: ${{ secrets.SENDKEY }}
        run: python push_logic.py
      
      - name: 提交进度
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add .push_state/
          git diff --cached --quiet || git commit -m "chore: 更新逻辑训练进度" && git push
